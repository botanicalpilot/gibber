import axios from 'axios';

export default class Search {
    constructor(query) {
        this.query
    }

    async getResults() {
        try{
            const res = await axios('');
            
        }
    }
}